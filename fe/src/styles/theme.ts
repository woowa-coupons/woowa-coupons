import { colors } from './base/Color';
import { fonts } from './base/Font';
import { shadow, radius, flex } from './base/Object';

export const theme = {
  colors,
  fonts,
  shadow,
  radius,
  flex,
};
